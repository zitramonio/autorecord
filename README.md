# autorecord
An application that automatically starts audio recording when a calendar event begins, with optional manual recording support. After recording stops, it transcribes the conversation, performs speaker diarization (separates speakers), and saves the output in TXT, MD, SRT, and JSON formats.
